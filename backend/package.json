{
  "name": "ucp-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start:dev",
    "start:dev": "set NODE_ENV=development&&ts-node-dev --respawn --transpile-only --ignore-watch node_modules --exit-child src/server.ts",
    "start:stg": "set NODE_ENV=staging&&ts-node-dev --respawn --transpile-only --ignore-watch node_modules --exit-child src/server.ts",
    "start:prd": "set NODE_ENV=production&&ts-node-dev --respawn --transpile-only --ignore-watch node_modules --exit-child src/server.ts",
    "build": "tsc -b"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/compression": "^1.7.0",
    "@types/cors": "^2.8.10",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.11",
    "@types/http-status-codes": "^1.2.0",
    "@types/nodemailer": "^6.4.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^8.6.0",
    "express": "^4.17.1",
    "express-validator": "^6.10.0",
    "http-status-codes": "^2.1.4",
    "moment-timezone": "^0.5.33",
    "nodemailer": "^6.6.0",
    "path": "^0.12.7",
    "winston": "^3.3.3"
  }
}
