import Layout from "@components/styled/Layout.styled";
import React from "react";

type PlanPageProps = {};

const PlanPage: React.FC<PlanPageProps> = () => {
  return <Layout.Centered>Start Planning</Layout.Centered>;
};

export default PlanPage;
